import React, { useState } from 'react';
import { Link } from 'react-router-dom';
export default function ForgotPassword() {
  const [email,setEmail]=useState('');const [err,setErr]=useState('');const [sent,setSent]=useState(false);const [loading,setLoading]=useState(false);
  const handle=(e)=>{e.preventDefault();if(!email){setErr('Email is required');return;}if(!/\S+@\S+\.\S+/.test(email)){setErr('Enter a valid email');return;}setErr('');setLoading(true);setTimeout(()=>{setSent(true);setLoading(false);},900);};
  return(<div className="auth-page"><div className="auth-left"><div style={{width:52,height:52,background:'var(--accent)',borderRadius:12,display:'flex',alignItems:'center',justifyContent:'center',fontSize:'1.5rem',color:'#fff',marginBottom:'1rem'}}><i className="bi bi-mortarboard-fill"></i></div><h2 style={{color:'#fff',fontFamily:'Syne,sans-serif',fontSize:'1.4rem'}}>Account Recovery</h2><p style={{color:'rgba(255,255,255,.4)',fontSize:'.875rem',lineHeight:1.7}}>Enter your registered email to receive a password reset link.</p></div><div className="auth-right"><div className="auth-form-box">{sent?(<div style={{textAlign:'center'}}><div style={{width:64,height:64,background:'rgba(5,150,105,.12)',borderRadius:'50%',display:'flex',alignItems:'center',justifyContent:'center',fontSize:'1.75rem',color:'#059669',margin:'0 auto 1.25rem'}}><i className="bi bi-envelope-check"></i></div><h2>Check your inbox</h2><p style={{color:'var(--text-secondary)'}}>Reset link sent to <strong>{email}</strong></p><Link to="/login" className="btn-accent d-inline-block mt-3" style={{borderRadius:10,padding:'.65rem 2rem'}}>Back to Sign In</Link></div>):(<><h2>Forgot password?</h2><p>We'll send a reset link to your email</p><form onSubmit={handle} noValidate><div className="mb-4"><label className="form-label">Email Address</label><input type="email" className={`form-control ${err?'is-invalid':''}`} placeholder="you@college.edu" value={email} onChange={e=>{setEmail(e.target.value);setErr('');}} />{err&&<div className="invalid-feedback">{err}</div>}</div><button type="submit" className="btn-accent w-100" style={{padding:'.75rem',borderRadius:10}} disabled={loading}>{loading?<><span className="spinner-border spinner-border-sm me-2"></span>Sending…</>:'Send Reset Link'}</button></form><p style={{textAlign:'center',marginTop:'1.5rem',fontSize:'.875rem',color:'var(--text-secondary)'}}>Remember your password? <Link to="/login" style={{fontWeight:600}}>Sign in</Link></p></>)}</div></div></div>);
}
